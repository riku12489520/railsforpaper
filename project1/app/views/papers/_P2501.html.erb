<div class="page">
		<h1>舊振南食品股份有限公司</h1>
		<h2>烤焙製程管制記錄表-旋轉爐(P2501)</h2>
		<span class="date"><%= date_format(@paper.created_at, "date")%></span>
		<span>
			<%= score_for_checked(@paper.score, name1="forming1",name2="forming2", test1="成型區(製程I)",test2="成型區(製程II)", el= :span) %>
		</span>
		<table class="table1">
			<thead>
				<tr class="th-row1">
					<th colspan="1" rowspan="2">車數</th>
					<th colspan="1" rowspan="2">烤焙前中心溫度</th>
					<th colspan="6">旋轉烤爐</th>
				</tr>
				<tr class="th-row2">
					<th>時間</th>
					<th>溫度(℃)</th>
					<th>風速(Hz)</th>
					<th>時間</th>
					<th>溫度(℃)</th>
					<th>風速(Hz)</th>
				</tr>
			</thead>
			<tbody>
        <% @papers.each do |t|%>
				<tr classs="td-row1">
					<td class="carId" rowspan="2"><%= t.id%></td>
					<td class="before-baking" rowspan="2"><%= t.id%>℃</td>
        <% t.stages.each do |s| %>
					<td class="time-start"><span>起</span><%= date_format(s.created_at, "time")%></td>
					<td class="temp" rowspan="2"><%= s.toptemp%>℃</td>
					<td class="wind" rowspan="2"><%= s.downtemp%>Hz</td>
        <% end %>
				</tr>
				<tr class="td-row2">
        <% t.stages.each do |s| %>
					<td class="time-end"><span>迄</span><%= date_format(s.updated_at, "time")%></td>
        <% end %>
				</tr>
        <% end %>
			</tbody>
		</table>
		<br />
		<table class="table2">
			<thead>
				<tr>
					<th class="col-1"></th>
					<th>領料日期</th>
					<th>有效日期</th>
				</tr>
			</thead>
			<tbody>
        <% @papers.each do |t|%>
				<tr>
					<td><%= t.item%></td>
					<td><%= date_format(t.created_at)%></td>
					<td><%= date_format(t.updated_at)%></td>
				</tr>
        <% end %>
			</tbody>
		</table>
		<div class="sign">
			<span class="supervisor">單位主管:<%= @paper.item %></span>
			<span class="recorder">填表人:<%= @paper.item %></span>
			<span class="version">Rev.1.0</span>
		</div>
	</div>